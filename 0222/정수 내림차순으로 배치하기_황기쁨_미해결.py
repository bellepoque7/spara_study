def solution(n):
    answer = 0
    return int(answer.reverse())

# reverse를 사용하면 뒤집기가 된다고 생각했는데 int라서 계속 안된다고 함.
# return int(str(answer).reverse())도 해보고
# return int(answer).revers()도 해보고 
# return int(answer.reverse())도 해봤지만 다 int라서 안된다고 했음. 
